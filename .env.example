# Credenciais do projeto Supabase (PRD-02).
# Copie para .env.local e preencha com os valores reais.
# O prefixo VITE_ é obrigatório: sem ele a variável não chega ao bundle.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
